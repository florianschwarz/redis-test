var assert = require('chai').assert;

describe('Redis tests', function () {

    before(function (done) {
        done();
    });


    after(function (done) {
        done();
    });

    beforeEach(function (done) {
        done();
    });

    afterEach(function (done) {
        done();
    });

    it('First mocha test', function (done) {
        assert.ok(true);
        done();
    });
});